services:
  backend:
    build:
      context: .
      dockerfile: dockerfiles/Dockerfile.backend
      target: dev
    ports:
      - "3001:3001"
    env_file:
      - ./apps/server/.env

  frontend:
    build:
      context: .
      dockerfile: dockerfiles/Dockerfile.frontend
      target: dev
    depends_on:
      - backend
    ports:
      - "5173:5173"
    env_file:
      - ./apps/client/.env
