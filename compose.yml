services:
  backend:
    build:
      context: ./apps/server
      target: dev
    ports:
      - 3001:3001

  frontend:
    build:
      context: ./apps/client
      target: dev
    depends_on:
      - backend
    ports:
      - 5173:5173
